"""Canonical Mosteller-derived item sets for the embeddings paper.

See `data.mosteller` for the actual definitions. This package exists so
experiment scripts can `from data.mosteller import ...`.
"""
